# Synthetic capability task set

This small offline reference set is separate from free Village activity. Tasks
use fixed token budgets and declared criteria; they are never injected into
resident prompts and are not used as a reward signal.

Initial categories are reproduction, recovery, coordination and memory. Every
run records task ID, condition ID, request/run IDs, outcome, elapsed time, token
budget and evidence references. Failed tasks remain valid results.
