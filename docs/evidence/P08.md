# Evidence P08: Transaktionaler Koordinationsspeicher (SQLite / Socket)

**Paket / Unterpaket:** P08
**Datum:** 2026-09-24T23:58:00+02:00
**Baseline-Revision und relevante bereits vorhandene Änderungen:**
- Git Repository Arbeitskopie: `/opt/deployment/ai-village`
- Vorgänger: P00 (`LOCAL_VERIFIED`), P01 (`HOST_VERIFIED`), P02 (`LOCAL_VERIFIED`), P03 (`LOCAL_VERIFIED`), P04 (`LOCAL_VERIFIED`), P05 (`LOCAL_VERIFIED`), P06 (`LOCAL_VERIFIED`), P07 (`LOCAL_VERIFIED`).
- Ausgangslage: Aufgaben wurden in `work-items.json` verwaltet. Bei Syntaxfehlern oder Crashs während Dateischreibvorgängen fiel der Runner auf ein leeres Array `[]` zurück und überschrieb den gesamten bisherigen Aufgabenbestand. Parallele Claims konnten bei flock-Kollisionen zu Inkonsistenzen führen.

**Diagnose mit Datei/Funktion oder datierter Host-Messung:**
- In `web/runtime.py` (`Tasks.operate`): `items = read_json(self.path, [])` ersetzte im Fehlerfall bestehende Aufgaben durch eine leere Liste.
- Fehlende Transaktionsisolation auf Zeilenebene; kein Versions- oder Migrationssystem.

**Implementierte Änderungen:**
1. Modul `village/coordinator.py`:
   - `CoordinationStore`: SQLite-gestützter ACID-Koordinationsspeicher (`coordination.sqlite3`) mit WAL-Modus (`journal_mode=WAL`), Busy-Timeouts und Transaktionen (`BEGIN IMMEDIATE`).
   - `schema_migrations`: Versionierte Migrationstabelle zur deterministischen Weiterentwicklung.
   - `tasks`-Tabelle: `id`, `title`, `success_criterion`, `author`, `owner`, `status`, `lease_until`, `evidence`, `completion_verified`, `version`, `created_at`, `updated_at`.
   - `messages`-Tabelle: Vorbereitung für Inbox/Ack mit `id`, `sender`, `recipient`, `reply_to`, `content`, `delivered`, `acknowledged`.
   - Atomare Race-Condition-freie Claims: Parallele Claim-Versuche auf dieselbe Task autorisieren genau einen Worker und lehnen konkurrierende Anfragen sauber ab.
   - Idempotenter Legacy-Import (`import_legacy_tasks`): Liest bestehende `work-items.json` ohne Dubletten ein.
   - Korruptions-Quarantäne: Ungültige/beschädigte JSON-Dateien werden in `corrupted-<name>.<timestamp>` isoliert, statt mit einem leeren Default überschrieben zu werden.
   - Kompatible Board-Projektion (`sync_projection`): Aktualisiert atomar die Datei `work-items.json` mit Rechten `0660`, wodurch bestehende Werkzeuge und Beobachter voll abwärtskompatibel bleiben.
2. `web/runtime.py`:
   - Klasse `Tasks` delegiert vollständig an `CoordinationStore`.
3. `village/release.py`:
   - `village/coordinator.py` in `RELEASE_FILES` aufgenommen.
4. i18n-Sprachdateien:
   - `locales/en.json` und `locales/de.json` um Koordinationsmeldungen synchron erweitert.

**Ausgeführte Tests, Befehle, Exitcodes und kurze Ergebnisse:**
- Lokale Unittests: `python3 -m unittest discover -s tests -v` (79 Tests, alle bestanden).
- `tests/test_coordinator.py`:
  - `test_schema_migrations_applied`: Migration 1 erfolgreich in `schema_migrations` erfasst.
  - `test_concurrent_claims_race_condition_safe`: Zwei konkurrierende Threads claimen gleichzeitig denselben Task. Genau ein Worker gewinnt, der andere erhält `ValueError: task already claimed`.
  - `test_idempotent_legacy_import`: Mehrfaches Importieren erzeugt keine Duplikate.
  - `test_corrupted_json_quarantine`: Beschädigte JSON-Dateien werden isoliert und nicht durch leere Defaults überschrieben.
  - `test_board_projection_sync`: `work-items.json` wird bei Task-Operationen atomar mit Rechten `0660` aktualisiert.
  - `test_task_limits_and_validation`: 64-Task-Limit und Validierungsregeln geprüft.
  - `test_file_permissions_restrictive`: Berechtigungen von `coordination.sqlite3` auf `0660` verifiziert.
- Git Diff Check: `git diff --check` (Exitcode 0).

**Nicht ausgeführte Tests und Grund:**
- Keine Schreibzugriffe auf Host N06-M10 (Directive: Simulation bleibt pausiert).

**Migration / Rollback / Auswirkungen auf andere Pakete:**
- Vollständige Kompatibilität: Existierende Tests und externe Werkzeuge können weiterhin `work-items.json` lesen.
- Grundlage für P09 (Dauerhafte Inbox und explizite Bestätigung) und P10 (Arbeitsfortschritt ohne Claim-Schleifen).

**Host-Freigabe, betroffene Ziele und Deployment-Nachweis:**
- Lokale Paketverifikation (`LOCAL_VERIFIED`). Keine Änderungen am Host.

**Simulation weiterhin pausiert? / .env unverändert?:**
- Ja, Simulation ist pausiert.
- Host `.env` bleibt unangetastet.

**Status:** `LOCAL_VERIFIED`
**Nächster erlaubter Schritt:** P09 – Dauerhafte Inbox und explizite Bestätigung (Inbox & Ack)
