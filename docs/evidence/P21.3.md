# P21.3 – Single active social round

The meeting scheduler now treats an existing open meeting as authoritative and
does not create another agenda after a service restart. Stale rounds are still
closed by the existing four-hour bound. This prevents overlapping social
interventions and makes meeting participation measurable.

Verification: scheduler policy, meeting and event tests are `LOCAL_VERIFIED`.
