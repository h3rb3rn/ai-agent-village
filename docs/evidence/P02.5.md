# P02.5 – Installer release-source guard

`scripts/install-runtime.py` now validates every source listed by
`village.release.RELEASE_FILES` before it reads the host configuration or makes
any installation change. Missing components fail closed. The backup manifest
also records the source revision and the complete release-source hash set.

Verification: installer guard tests and existing release tests are
`LOCAL_VERIFIED`. No host installation or service restart was performed.
