# Evidence P03: Einheitliche Konfiguration und Validierung

**Paket / Unterpaket:** P03
**Datum:** 2026-09-24T23:42:15+02:00
**Baseline-Revision und relevante bereits vorhandene Änderungen:**
- Git Repository Arbeitskopie: `/opt/deployment/ai-village`
- Vorgänger: P00 (`LOCAL_VERIFIED`), P01 (`HOST_VERIFIED`), P02 (`LOCAL_VERIFIED`).
- Ausgangslage: Konfiguration wurde bisher in Shell-Skripten mit rudimentären sed/regex-Mustern geparst. Es fehlte ein stark typisierter Vertrag mit Wertebereichs- und Portkollisionsprüfungen sowie aussagekräftigen Fehlermeldungen vor Systemstarts.

**Diagnose mit Datei/Funktion oder datierter Host-Messung:**
- Ungültige Ports (z.B. > 65535), Portkollisionen zwischen Diensten (z.B. WebUI vs. Memory Gateway), ungültige Kontextgrößen oder fehlerhafte Think-Levels führten zu unklaren Laufzeitfehlern oder stillschweigenden Fehlkonfigurationen.
- Fehlende strikte Validierung von `.env`-Dateien gefährdete den konsistenten Betrieb der 9 Inferenz-Lanes.

**Implementierte Änderungen:**
1. Modul `village/config.py`:
   - Stark typisierte Dataclasses: `VillageConfig` und `AgentConfig`.
   - Strenge Bereichs- und Vertragskontrollen:
     - `cycle_seconds`: 1 .. 86400
     - `command_timeout_seconds`: 0 .. 86400
     - `ollama_timeout_seconds`: 5 .. 14400
     - `offline_retry_seconds`: 1 .. 3600
     - `max_output_bytes`: 512 .. 1048576
     - `webui_port` und `memory_port`: 1 .. 65535 mit Prüfung auf Portkollision
     - `think_level`: strikt beschränkt auf `off`, `low`, `medium`, `high`, `max`
     - `keep_alive`: Formatvalidierung (`10m`, `24h`, `0`, `-1`)
     - `role`: validiert gegen erlaubte Rollen (`resident`, `builder`, `steward`, `king`, `specialist`)
     - `num_ctx`: 512 .. 262144
     - `num_predict`: 32 .. 32768
   - Unterstützung für gepaddete (`OLLAMA_AGENT_01_`) und ungepaddete (`OLLAMA_AGENT_1_`) Präfixe.
   - CLI-Tool `python3 -m village.config` mit Validierungs- und JSON-Export-Modus.
2. Integration in `village/release.py`:
   - `village/config.py` in `RELEASE_FILES` aufgenommen.
3. `.env.example`:
   - Vollständig validierbar mit `village/config.py` ohne Secrets oder echte Schlüssel.

**Ausgeführte Tests, Befehle, Exitcodes und kurze Ergebnisse:**
- Lokale Unittests: `python3 -m unittest discover -s tests -v` (47 Tests, alle bestanden, inkl. 9 Tests in `tests/test_config.py`).
- CLI-Test: `python3 -m village.config .env.example` -> Exitcode 0 (`Configuration valid: .env.example`).
- Fehlerfälle:
  - Port außerhalb des Bereichs (> 65535) -> `ConfigValidationError`
  - Portkollision (`webui_port == memory_port`) -> `ConfigValidationError`
  - Ungültiges Think-Level (`ultra`) -> `ConfigValidationError`
  - Ungültige Rolle (`warlord`) -> `ConfigValidationError`
  - Ungültiges Context-Window (< 512) -> `ConfigValidationError`
- Syntax- und Lint-Checks: `git diff --check`, `bash -n ...`, `node --check ...` (alle Exitcode 0).

**Nicht ausgeführte Tests und Grund:**
- Host `.env` auf N06-M10 wurde nicht gelesen oder modifiziert (striktes Schutz-Gate: Host-.env wird vom Betreiber bearbeitet).

**Migration / Rollback / Auswirkungen auf andere Pakete:**
- Liefert das Fundament für P04 (Provideradapter für Ollama und OpenAI-kompatible Schnittstellen).
- Rollback: `village/config.py` ist rein additiv und berührt keine bestehende Host-Konfiguration.

**Host-Freigabe, betroffene Ziele und Deployment-Nachweis:**
- Keine Host-Schreiboperationen für dieses Paket erforderlich.

**Simulation weiterhin pausiert? / .env unverändert?:**
- Ja, Simulation ist pausiert.
- Host `.env` wurde weder synchronisiert noch verändert.

**Status:** `LOCAL_VERIFIED`
**Nächster erlaubter Schritt:** P04 – Ollama- und OpenAI-kompatibler Inferenzadapter
