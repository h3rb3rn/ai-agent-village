# P24.3 – Host-Reboot mit persistentem Pausezustand

## HOST_VERIFIED – N06-M10, 2026-09-26

- Reboot über `ssh N06-M10 "sudo reboot"` ausgeführt.
- Neuer Bootzeitpunkt: `2026-09-26 16:04 CEST`.
- `/etc/ai-village/paused` blieb erhalten; `village.control status` meldet
  `paused=true`, Modus `drain`.
- Alle neun Agent-Units sind nach dem Boot `inactive/dead`; kein Agent wurde
  durch den Reboot automatisch gestartet.
- Authority, WebUI, Telemetrie und Memory Gateway sind aktiv.
- Neun alte `active_request.json`-Dateien bleiben als Recovery-Artefakte erhalten;
  sie wurden nicht stillschweigend gelöscht oder als erfolgreich abgeschlossen
  interpretiert.

## Einschränkung

Dies war ein Pause-Reboot-Test. Ein freigegebener Running-Reboot mit anschließendem
kontrolliertem Canary-Restore ist weiterhin offen.
