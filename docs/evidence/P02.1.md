# P02.1 – Canonical bootstrap overlay

## Scope

The repository implementations of `web/telemetry-collector.py` and
`web/webui.py` now overwrite the historical embedded bootstrap fallbacks after
generation. A fresh install therefore uses the reviewed repository sources.

## Verification

- `bash -n bootstrap-ai-village.sh` — `LOCAL_VERIFIED`
- `python3 -m unittest tests.test_canonical_bootstrap -q` — `LOCAL_VERIFIED`
- No host installation or service restart was performed in this package.

## Remaining P02 work

The embedded authority and helper scripts still need extraction into canonical
files. They are intentionally outside this bounded subpackage.
