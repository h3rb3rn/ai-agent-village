# P22.2 – Sichtbare Projektionszustände im Dashboard

Die Gedächtnis-Sektion stellt ChromaDB-/Neo4j-Projektionen jetzt als eigene
Karten dar. Je Backend werden Status, Outbox-Rückstand und Fehlerzähler gezeigt.
Fehlt die Projektion, wird das explizit als „noch keine Backends registriert“
angezeigt; Primärspeicher wird nicht als semantische Indexierung ausgegeben.

## Nachweis

- `python3 -m unittest tests.test_dashboard_memory_projection tests.test_memory_stats -v` – LOCAL_VERIFIED
- `node --check web/observatory.js` und `git diff --check` erfolgreich.
- Keine Host-Änderung und kein Simulationsstart in diesem Paket.

## Offen

Host-Rollout, Browser-Abnahme mit realen Chroma-/Neo4j-Zuständen und mobile
Layoutprüfung bleiben Teil der P22-Abnahme.
