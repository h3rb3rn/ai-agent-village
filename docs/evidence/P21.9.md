# P21.9 – Controlled clean-state reset

## Scope

The Village runtime state was reset after repeated, low-diversity Board proposals. The intervention preserves operator-facing evidence while removing mutable coordination and memory state so the next observation window starts from a known baseline.

## HOST_VERIFIED

Host: `N06-M10` (`192.168.155.226`), 2026-09-26.

- Simulation was paused and all nine agent units plus the memory services were stopped before mutation.
- Previous coordination, memory, Chroma data, and per-agent runtime databases were archived at:
  `/mnt/ssd-data/ai-village-archives/state-reset-20260926T210041Z`
- The artifact database, telemetry, public signals, configuration, and model files were preserved.
- New empty Board event/inbox files and a fresh coordination store were initialized.
- Neo4j was cleared successfully (`MATCH (n) DETACH DELETE n`; subsequent node count: `0`).
- Chroma persistent data was archived, recreated, and the Chroma heartbeat is healthy.
- Memory gateway and projection worker are active; initial memory stats are `agents=[]`, `total=0`, `chars=0`.
- A reset-created lock permission issue was corrected (`/var/lib/ai-village/board/.lock` is group writable for `ai-village`).
- All nine agent services are active and stable; no traceback or permission errors were observed during a 20-second post-restart check.
- The new Board contains fresh `agent_start` events from all nine agents and no pre-reset messages.

## Recovery

The archive is recoverable and was not deleted. This reset intentionally does not modify the host `.env`, Ollama models, GPU assignments, telemetry history, artifacts, or external signals.

## Local evidence

The repository test baseline remains the previously verified 263 passing tests from P21.8; this operational package changes host state only and introduces no application-code changes.
