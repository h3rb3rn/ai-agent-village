# P06.3 – Shell- und Telemetrie-Produzenten normalisiert

Lifecycle-Ereignisse von `village-resume` und `village-update` verwenden jetzt
den installierten gemeinsamen Append-Helper. Telemetrie-Snapshots werden mit
demselben Event-Umschlag in `telemetry/events.jsonl` geschrieben; die SQLite-
Historie und `latest.json` bleiben unverändert.

## Nachweis

- `python3 -m unittest tests.test_event_producers_shell tests.test_event_producers tests.test_events -v` – LOCAL_VERIFIED
- Python-Kompilierung und `git diff --check` erfolgreich.
- Keine Host-Änderung und kein Simulationsstart in diesem Paket.

## Hinweis

Die Shell-Hooks benötigen beim nächsten Release-Rollout den neuen Helper unter
`/usr/local/lib/ai-village/append-event.py`; ein Host-Rollout ist daher noch offen.
