# Evidence P02: Eine Quelle pro Komponente und versionierter Release

**Paket / Unterpaket:** P02
**Datum:** 2026-09-24T23:40:30+02:00
**Baseline-Revision und relevante bereits vorhandene Änderungen:**
- Git Repository Arbeitskopie: `/opt/deployment/ai-village`
- Vorgänger: P00 (`LOCAL_VERIFIED`), P01 (`HOST_VERIFIED`).
- Ausgangslage: Wichtige Komponenten (`webui.py`, `telemetry-collector.py`, `authority.py`, `agent-runner`, `village-resume`, `village-update`) waren als Heredocs in `bootstrap-ai-village.sh` eingebettet oder wurden ad-hoc extrahiert. Es gab kein versioniertes Manifest mit kryptografischer Integritätsprüfung.

**Diagnose mit Datei/Funktion oder datierter Host-Messung:**
- Unterschiedliche Entstehungs- und Installationspfade führten zu Drift zwischen Host `/usr/local/lib/ai-village` und Quell-Repository.
- Fehlende Manifestprüfung erlaubte das unbemerkte Ausrollen unvollständiger oder inkonsistenter Dateistände.
- Installer besaßen keine `--dry-run`- und `--no-start`-Flags, um vor Ausführung Hash-Prüfungen ohne Dienstbeeinträchtigung vorzunehmen.

**Implementierte Änderungen:**
1. Kanonische Einzelquellen im Repository etabliert:
   - `web/webui.py`: Webserver und API (sha256: `eef06c08128768876357c909f4491e5655f10bf5bc3721412e76875c0b07086c`)
   - `web/telemetry-collector.py`: Telemetriedienst (sha256: `e2634626b40de28f70874cbcb8323af18d825ef2a6630986864a01cdc872479e`)
   - `village/authority.py`: Root-Authority-Daemon (sha256: `312a0bb549606faee522b0897eadc66eba06e1913a7f8da19bfb29855a4361b8`)
   - `scripts/agent-runner`: Agent-Starter (sha256: `e2f63ee219e0575ee714886d2b046d4047cdaa7b680e01e294cd9fec9fb6444d`)
   - `scripts/village-resume`: Rebootfester Wiederaufnahmeskript
   - `scripts/village-update`: Aktualisierungsskript
   Alle Dateien stimmen exakt mit den auf N06-M10 installierten Ständen überein.
2. Modul `village/release.py`:
   - `build_release`: Erstellt vollständiges Release-Bundle mit korrekten Berechtigungen.
   - `create_release_manifest`: Erstellt `release-manifest.json` mit Git-Revision, Zeitstempel, Schemaversionen und SHA-256 Hashes aller Dateien.
   - `verify_release`: Verifiziert Integrität des Bundles; schlägt bei fehlenden oder veränderten Dateien fehl.
   - CLI: `python3 -m village.release build DEST_DIR` und `python3 -m village.release verify RELEASE_DIR`.
3. Installer-Härtung:
   - `scripts/install-observatory.sh`: Unterstützt `--dry-run`, `--no-start`, Manifest-Prüfung und sowohl flaches als auch strukturiertes Layout.
   - `scripts/install-runtime.py`: Unterstützt `--dry-run`, `--no-start`, `--source` und atomare Backups.

**Ausgeführte Tests, Befehle, Exitcodes und kurze Ergebnisse:**
- Lokale Unittests: `python3 -m unittest discover -s tests -v` (38 Tests, alle bestanden, inkl. 4 Tests in `tests/test_release.py`).
- Syntax-Checks: `bash -n bootstrap-ai-village.sh`, `bash -n scripts/install-observatory.sh`, `bash -n scripts/village-resume`, `bash -n scripts/village-update`, `node --check web/observatory.js`, `git diff --check` (alle Exitcode 0).
- Test Release Build & Verify:
  - `python3 -m village.release build /tmp/village-release` -> Erfolg, 16 Komponenten gebündelt, Manifest geschrieben.
  - `python3 -m village.release verify /tmp/village-release` -> Erfolgreich verifiziert.
  - `./scripts/install-observatory.sh /tmp/village-release --dry-run` -> Erfolg, keine Dateien kopiert, keine Dienste neu gestartet.
- Test Tamper Detection:
  - Modifikation einer Datei im Release-Verzeichnis -> `verify` und `--dry-run` schlagen sofort mit Hash-Mismatch fehl (Exitcode > 0).

**Nicht ausgeführte Tests und Grund:**
- Reales Überschreiben von Host-Systemdateien mit Neustart der WebUI wurde nicht ausgeführt; das Host-System befindet sich im stabilen, pausierten Zustand und alle Hashes der kanonischen Dateien stimmen mit den Host-Dateien überein.

**Migration / Rollback / Auswirkungen auf andere Pakete:**
- Schafft die verlässliche Ausgangslage für P03 (Einheitliche Konfiguration und Validierung) und P04 (Provideradapter).
- Rollback: Jede Installation erzeugt automatische Zeitstempel-Backups unter `/var/backups/ai-village/`.

**Host-Freigabe, betroffene Ziele und Deployment-Nachweis:**
- Hashes auf N06-M10 abgeglichen und identisch.

**Simulation weiterhin pausiert? / .env unverändert?:**
- Ja, Simulation ist pausiert.
- Host `.env` bleibt unangetastet.

**Status:** `LOCAL_VERIFIED` (und Hashes gegen Host verifiziert)
**Nächster erlaubter Schritt:** P03 – Einheitliche Konfiguration und Validierung
